{% extends 'base.html.twig' %}


{% block body %}
    {% embed 'inc/new.html.twig'%}
        {% block list %}
            <ol class="breadcrumb">
                <li><a href="/">Home</a></li>
                <li class="active">Dodaj klub</li>
            </ol>
        {% endblock %}
        {% block page_title %} Dodaj klub {% endblock %}
        {% block form %}
            {{ form_start(form) }}
            <div class="form-group">
                {{ form_label(form.club_name) }}
                {{ form_widget(form.club_name) }}
            </div>
            <div class="form-group">
                {{ form_label(form.league_id) }}
                {{ form_widget(form.league_id) }}
            </div>
            <div class="form-group">
                {{ form_label(form.stadium) }}
                {{ form_widget(form.stadium) }}
            </div>
            <div class="form-group">
                {{ form_label(form.founded_year) }}
                {{ form_widget(form.founded_year) }}
            </div>
            <br>
            <div class="row">
                <div class="col-lg-12 text-right">
                    {{ form_label(form.save) }}
                    {{ form_widget(form.save) }}
                </div>
            </div>
            <br>
            {{ form_end(form) }}

            {% if errors|length %}
                <br>
                <div class="alert alert-danger">
                    <ul>
                        {% for error in errors %}
                            <li>{{ error.message }}</li>
                        {% endfor %}
                    </ul>
                </div>
            {% endif %}
        {% endblock %}
    {% endembed %}
{% endblock %}